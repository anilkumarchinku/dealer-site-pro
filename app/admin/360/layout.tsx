// Maintainer 360 command center — wears the landing palette (warm cream/ink/bronze).
export default function Admin360Layout({ children }: { children: React.ReactNode }) {
    return <div className="dsp-app-skin min-h-screen">{children}</div>;
}
